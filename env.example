# Copy to your shell or EC2 User Data (never commit real secrets).
# PostgreSQL on Amazon RDS — values must match what you created in RDS.

SPRING_PROFILES_ACTIVE=prod
DB_HOST=your-db.xxxxxxxxxxxx.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASS=
