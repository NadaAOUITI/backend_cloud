# Local / EC2: set these when USE_JSON_STORAGE is not enabled (RDS MySQL).
# Never commit real secrets. On AWS, inject via User Data / Systems Manager / env file.

USE_JSON_STORAGE=false
DB_HOST=your-db.xxxxxxxxxxxx.region.rds.amazonaws.com
DB_USER=admin
DB_PASSWORD=
DB_NAME=appdb
